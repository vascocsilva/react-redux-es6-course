import React, {PropTypes} from 'react';

const SelecInput = ({name, label, onChange, defaultOption, value, error, options}) => 
  return (
    <div className="form-group">
      <label htmlFor={name}>{label}</label>
      <div className="field">
        <select
          name={name}
          value=
      </div>
    </div>
  );
};
